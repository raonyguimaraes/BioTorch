#class to parse Snpedia content to find relation betwen SNPs, Medical Conditions and Medicines



class Parse_snpedia:
  def find_snp(description): # return Fibonacci series up to n
      """Return a list containing the Fibonacci series up to n."""
      result = []
      a, b = 0, 1
      while a < n:
	  result.append(a)    # see below
	  a, b = b, a+b
      return result
     


